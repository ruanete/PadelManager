build:
  docker:
    web: Dockerfile
run:
  web: java -Dserver.port=$PORT $JAVA_OPTS -Dspring-boot.run.profiles=prod -Dspring-boot.run.arguments="--jasypt.encryptor.password=$DECRYPT_KEY" -jar target/*.jar
